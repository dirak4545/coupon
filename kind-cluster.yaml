kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: coupon

nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
        listenAddress: 127.0.0.1

  - role: worker
  - role: worker